package com.lala.guidview.guid.listener

import android.view.View
import com.lala.guidview.guid.core.GuideContrller

interface OnLayoutInflatedListener {
    fun onLayoutInflated(view: View,contrller: GuideContrller )
}